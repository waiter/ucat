<template name="card">
  <view class="card_view">
    <image bindtap="showPic" data-show="{{p}}" src="{{p}}" class="card_pic"></image>
    <button data-show="{{p}}" class="card_copy" type="primary" catchtap="showPic">点击后长按识别二维码</button>
  </view>
</template>